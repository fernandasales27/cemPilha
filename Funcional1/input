let var s = emptyStack in
  let var s1 = push(s, 1) in      
    let var s2 = push(s1, 2) in   
      let var v = top(s2) in
        let var s3 = pop(s2) in
          let var v2 = top(s3) in
            v + v2